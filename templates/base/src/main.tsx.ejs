<% if (!addons.includes('react-router-framework') && !addons.includes('tanstack-start')) { %>
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import "./index.css";
<% if (addons.includes('tanstack-query')) { %>
import { QueryClientProvider } from "@tanstack/react-query";
import { queryClient } from "./lib/query-client";
<% } %>
<% if (addons.includes('react-router') || !addons.includes('tanstack-router')) { %>
import App from "./App";
<% } %>

<% if (addons.includes('react-router')) { %>
import { BrowserRouter } from "react-router-dom";
<% } else if (addons.includes('tanstack-router')) { %>
import { RouterProvider } from "@tanstack/react-router";
import { router } from "./router";
<% } %>

createRoot(document.getElementById("root") as HTMLElement).render(
	<StrictMode>
		<% if (addons.includes('tanstack-query')) { %>
		<QueryClientProvider client={queryClient}>
		<% } %>
		<% if (addons.includes('react-router')) { %>
		<BrowserRouter>
			<App />
		</BrowserRouter>
		<% } else if (addons.includes('tanstack-router')) { %>
		<RouterProvider router={router} />
		<% } else { %>
		<App />
		<% } %>
		<% if (addons.includes('tanstack-query')) { %>
		</QueryClientProvider>
		<% } %>
	</StrictMode>
);
<% } %>
