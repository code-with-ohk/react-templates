<% if (queries.tanstackRouter || queries.tanstackStart) { %>
import { createFileRoute } from '@tanstack/react-router'
import { createClient } from '@supabase/supabase-js'

const supabase = createClient(import.meta.env.VITE_SUPABASE_URL, import.meta.env.VITE_SUPABASE_ANON_KEY)

export const Route = createFileRoute('/login')({ component: LoginRoute })

function LoginRoute() {
  return <div className="flex min-h-screen w-full items-center justify-center px-4 py-10">Login (Supabase)</div>
}
<% } %>
