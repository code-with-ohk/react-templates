<% if (queries.reactRouterFramework) { %>
import { RequireAuth } from '../../src/auth/require-auth'

export default function ProtectedPage() {
  return (
    <RequireAuth>
      <div className="flex min-h-screen w-full items-center justify-center px-4 py-10">
        <div className="w-full max-w-md rounded-2xl border border-border bg-background/80 p-6 shadow-sm backdrop-blur">
          <div className="space-y-2">
            <p className="text-xs font-medium uppercase tracking-[0.2em] text-muted-foreground">Protected area</p>
            <h2 className="text-2xl font-semibold tracking-tight">You are authenticated</h2>
            <p className="text-sm text-muted-foreground">This page is only visible to signed-in users.</p>
          </div>
        </div>
      </div>
    </RequireAuth>
  )
}
<% } %>
