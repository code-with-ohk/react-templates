import { Link } from "react-router-dom";

export default function DashboardIndex() {
	return (
		<div className="text-center">
			<p className="<%- addons.includes('tailwind') ? 'text-muted-foreground' : '' %>">
				This is the nested dashboard home.
			</p>
			<Link to="/dashboard/settings" className="<%- addons.includes('tailwind') ? 'mt-3 inline-block text-blue-500 hover:underline' : '' %>">
				Go to settings
			</Link>
		</div>
	);
}